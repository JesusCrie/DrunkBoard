import { AlcoholRatePipe } from './alcohol-rate.pipe';

describe('AlcoholRatePipe', () => {
  it('create an instance', () => {
    const pipe = new AlcoholRatePipe();
    expect(pipe).toBeTruthy();
  });
});
